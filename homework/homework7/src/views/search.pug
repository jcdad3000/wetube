extends layout
include mixins/movie

block content
  form(method="GET")
    input(type="text",name="keyword",placeholder = "Movie Title")
    input(type="submit",value="Search")
  div 
    each movie in movies
      +movieMixin(movie)